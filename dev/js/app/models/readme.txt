There are all our data into obj 'data'.

'data'-map:
	1) 'data.categories' - here are outlays & incomes categories (arrays).
			for example: 'data.categories.outlays' - get arr 'outlays';
						 'data.categories.incomes[2]' - get 3-item of arr 'insomes';

	2) 'data.regular', all data: 
			'data.regular.day.outlays';
			'data.regular.month.incomes.inc0'

	3) 'data.forecast', 
			'data.forecast.month'